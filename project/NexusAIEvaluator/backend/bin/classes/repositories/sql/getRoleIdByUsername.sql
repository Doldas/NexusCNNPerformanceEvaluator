-- $1 = username
-- $2 = rolename
SELECT USER_ROLES.ROLEID
FROM USER_ROLES
INNER JOIN USERS ON LOWER(USERS.USERNAME) = LOWER($1)
AND USER_ROLES.USERID = USERS.USERID
INNER JOIN ROLES ON LOWER(ROLES.ROLENAME) = LOWER($2)
AND USER_ROLES.ROLEID = ROLES.ROLEID
